# Public sample notice

The code in `examples/` was written for public technical evaluation.

It does not contain the production Passárgada source code, booking-engine credentials, guest information, analytics IDs, internal reports or private operational configuration.

All endpoint values, identifiers and test data are synthetic.
